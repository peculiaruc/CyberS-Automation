# Functions are reusable blocks of code that perform specific tasks. function is define using def()

# Example

def name(greet):
    """This function names a person to greet"""
    message = "Pec, " + greet + "!"
    return message

# Calling the name function
print(name("Hi"))
